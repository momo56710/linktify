import React from 'react'
import { auth } from "@/utils/firebase";
import { signOut } from 'firebase/auth'

export default function User({ email, name, profession }) {
   
    return (
        <div></div>

    )
}
